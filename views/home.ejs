<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Welcome to My Application</h1>
        <%- include('partials/navigation') %>
    </header>
    <main>
    <% if (session.username) { %>
        <h2>Welcome back, <%= session.username %>!</h2>
    <% } %>

    <!-- New Section for External API Data -->
    <div class="api-data">
        <h3>A Fun Fact for You</h3>
        <p><em><%= catFact %></em></p>
    </div>

    <hr>

    <!-- New Section for Displaying Database Posts -->
    <h2>Recent Posts</h2>
    <% if (posts.length > 0) { %>
        <div class="posts-list">
            <% posts.forEach(post => { %>
                <article class="post">
                    <h3><%= post.title %></h3>
                    <p class="post-meta">by <%= post.username %> on <%= new Date(post.created_at).toLocaleDateString() %></p>
                    <p><%= post.content %></p>
                </article>
            <% }); %>
        </div>
    <% } else { %>
        <p>No posts have been made yet. Be the first!</p>
    <% } %>
</main>
</body>
</html>